scripted_gui = {

	economic_test_ui = {
		context_type = player_context
		window_name = "countryeconomyview"

		visible = {
			check_variable = { ebf_economy_show = 1 }
		}

		effects = {
			close_button_click = {
				set_variable = { ebf_economy_show = 0 }
			}

			production_header_bg_click = {
				if = {
					limit = { 
						check_variable = { ebf_economy_extended_show = 1 } 
					}
					set_variable = { ebf_economy_extended_show = 0 }
				}
				else = {
					set_variable = { ebf_economy_extended_show = 1 }
				}
			}

			### Income Tax Percentage Click
			income_tax_sub_button_click = {
				add_political_power = ebf_economy_pp_base_cost
				
				set_temp_variable = { ebf_economy_change_income_tax = 0.01 }
				adjust_income_tax = yes
            }

			income_tax_add_button_click = {
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_income_tax > 0.99 }
						}
					}
					add_political_power = ebf_economy_pp_base_cost

					add_to_variable = { ebf_economy_income_tax = 0.01 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
					happines = yes
				}
            }

			income_tax_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_income_tax < 0.05 }
					}
					
					set_temp_variable = { ebf_economy_pp_cost = ebf_economy_income_tax }
					multiply_temp_variable = { ebf_economy_pp_cost = 100 }
					multiply_temp_variable = { ebf_economy_pp_cost = ebf_economy_pp_base_cost }
					add_political_power = ebf_economy_pp_cost

					set_variable = { ebf_economy_income_tax = 0.00 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
					happines = yes

				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_income_tax < 0.05 }
						}
					}
					set_temp_variable = { ebf_economy_pp_cost = 5 }
					multiply_temp_variable = { ebf_economy_pp_cost = ebf_economy_pp_base_cost }
					add_political_power = ebf_economy_pp_cost

					subtract_from_variable = { ebf_economy_income_tax = 0.05 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
					happines = yes

				
				}
            }
			income_tax_add_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_income_tax > 0.95 }
					}
					set_temp_variable = { ebf_economy_pp_cost = 1 }
					subtract_from_temp_variable = { ebf_economy_pp_cost = ebf_economy_income_tax }
					multiply_temp_variable = { ebf_economy_pp_cost = 100 }
					multiply_temp_variable = { ebf_economy_pp_cost = ebf_economy_pp_base_cost }
					add_political_power = ebf_economy_pp_cost

					set_variable = { ebf_economy_income_tax = 1.00 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
					happines = yes

					
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_income_tax > 0.95 }
						}
					}
					set_temp_variable = { ebf_economy_pp_cost = 5 }
					multiply_temp_variable = { ebf_economy_pp_cost = ebf_economy_pp_base_cost }
					add_political_power = ebf_economy_pp_cost

					add_to_variable = { ebf_economy_income_tax = 0.05 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
					happines = yes

			
				}
            }

			income_tax_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_income_tax < 0.1 }
					}
					set_temp_variable = { ebf_economy_pp_cost = ebf_economy_income_tax }
					multiply_temp_variable = { ebf_economy_pp_cost = 100 }
					multiply_temp_variable = { ebf_economy_pp_cost = ebf_economy_pp_base_cost }
					add_political_power = ebf_economy_pp_cost

					set_variable = { ebf_economy_income_tax = 0.00 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
					happines = yes

				
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_income_tax < 0.1 }
						}
					}
					set_temp_variable = { ebf_economy_pp_cost = 10 }
					multiply_temp_variable = { ebf_economy_pp_cost = ebf_economy_pp_base_cost }
					add_political_power = ebf_economy_pp_cost

					subtract_from_variable = { ebf_economy_income_tax = 0.1 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
					happines = yes

				}
            }
			income_tax_add_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_income_tax > 0.90 }
					}
					set_temp_variable = { ebf_economy_pp_cost = 1 }
					subtract_from_temp_variable = { ebf_economy_pp_cost = ebf_economy_income_tax }
					multiply_temp_variable = { ebf_economy_pp_cost = 100 }
					multiply_temp_variable = { ebf_economy_pp_cost = ebf_economy_pp_base_cost }
					add_political_power = ebf_economy_pp_cost

					set_variable = { ebf_economy_income_tax = 1.00 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
					happines = yes

				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_income_tax > 0.90 }
						}
					}
					set_temp_variable = { ebf_economy_pp_cost = 10 }
					multiply_temp_variable = { ebf_economy_pp_cost = ebf_economy_pp_base_cost }
					add_political_power = ebf_economy_pp_cost

					add_to_variable = { ebf_economy_income_tax = 0.1 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
					happines = yes

				
				}
            }

			income_tax_sub_button_right_click = {
				set_temp_variable = { ebf_economy_pp_cost = ebf_economy_default_income_tax }
				subtract_from_temp_variable = { ebf_economy_pp_cost = ebf_economy_income_tax }
				if = {
					limit = {
						check_variable = { ebf_economy_pp_cost < 0 }
					}
					multiply_temp_variable = { ebf_economy_pp_cost = -1 }
				}
				multiply_temp_variable = { ebf_economy_pp_cost = 100 }
				multiply_temp_variable = { ebf_economy_pp_cost = ebf_economy_pp_base_cost }
				add_political_power = ebf_economy_pp_cost

				set_variable = { ebf_economy_income_tax = ebf_economy_default_income_tax }
				rich_population = yes
				combined_tax = yes
				monthly_balance = yes
				happines = yes

            }
			income_tax_add_button_right_click = {
				set_temp_variable = { ebf_economy_pp_cost = ebf_economy_default_income_tax }
				subtract_from_temp_variable = { ebf_economy_pp_cost = ebf_economy_income_tax }
				if = {
					limit = {
						check_variable = { ebf_economy_pp_cost < 0 }
					}
					multiply_temp_variable = { ebf_economy_pp_cost = -1 }
				}
				multiply_temp_variable = { ebf_economy_pp_cost = 100 }
				multiply_temp_variable = { ebf_economy_pp_cost = ebf_economy_pp_base_cost }
				add_political_power = ebf_economy_pp_cost
				
				set_variable = { ebf_economy_income_tax = ebf_economy_default_income_tax }
				rich_population = yes
				combined_tax = yes
				monthly_balance = yes
				happines = yes
            }

			### Consumtion Tax Percentage Click
			consumption_tax_sub_button_click = {
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_consumption_tax < 0.01 }
						}
					}
					subtract_from_variable = { ebf_economy_consumption_tax = 0.01 }
					combined_tax = yes
					monthly_balance = yes
					happines = yes
				}
            }
			consumption_tax_add_button_click = {
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_consumption_tax > 0.99 }
						}
					}
					add_to_variable = { ebf_economy_consumption_tax = 0.01 }
					combined_tax = yes
					monthly_balance = yes
					happines = yes
				}
            }

			consumption_tax_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_consumption_tax < 0.05 }
					}
					set_variable = { ebf_economy_consumption_tax = 0.00 }
					combined_tax = yes
					monthly_balance = yes
					happines = yes
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_consumption_tax < 0.05 }
						}
					}
					subtract_from_variable = { ebf_economy_consumption_tax = 0.05 }
					combined_tax = yes
					monthly_balance = yes
					happines = yes
				}
            }
			consumption_tax_add_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_consumption_tax > 0.95 }
					}
					set_variable = { ebf_economy_consumption_tax = 1.00 }
					combined_tax = yes
					monthly_balance = yes
					happines = yes
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_consumption_tax > 0.95 }
						}
					}
					add_to_variable = { ebf_economy_consumption_tax = 0.05 }
					combined_tax = yes
					monthly_balance = yes
					happines = yes
				}
            }

			consumption_tax_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_consumption_tax < 0.1 }
					}
					set_variable = { ebf_economy_consumption_tax = 0.00 }
					combined_tax = yes
					monthly_balance = yes
					happines = yes
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_consumption_tax < 0.1 }
						}
					}
					subtract_from_variable = { ebf_economy_consumption_tax = 0.1 }
					combined_tax = yes
					monthly_balance = yes
					happines = yes
				}
            }
			consumption_tax_add_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_consumption_tax > 0.90 }
					}
					set_variable = { ebf_economy_consumption_tax = 1.00 }
					combined_tax = yes
					monthly_balance = yes
					happines = yes
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_consumption_tax > 0.90 }
						}
					}
					add_to_variable = { ebf_economy_consumption_tax = 0.1 }
					combined_tax = yes
					monthly_balance = yes
					happines = yes
				}
            }

			consumption_tax_sub_button_right_click = {
				set_variable = { ebf_economy_consumption_tax = ebf_economy_default_consumption_tax }
				rich_population = yes
				combined_tax = yes
				monthly_balance = yes
				happines = yes

            }
			consumption_tax_add_button_right_click = {
				set_variable = { ebf_economy_consumption_tax = ebf_economy_default_consumption_tax }
				rich_population = yes
				combined_tax = yes
				monthly_balance = yes
				happines = yes
            }

			### Wealth Tax Percentage Click
			wealth_tax_sub_button_click = {
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_wealth_tax < 0.01 }
						}
					}
					subtract_from_variable = { ebf_economy_wealth_tax = 0.01 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
				}
            }
			wealth_tax_add_button_click = {
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_wealth_tax > 0.99 }
						}
					}
					add_to_variable = { ebf_economy_wealth_tax = 0.01 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
				}
            }

			wealth_tax_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_wealth_tax < 0.05 }
					}
					set_variable = { ebf_economy_wealth_tax = 0.00 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_wealth_tax < 0.05 }
						}
					}
					subtract_from_variable = { ebf_economy_wealth_tax = 0.05 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
				}
            }
			wealth_tax_add_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_wealth_tax > 0.95 }
					}
					set_variable = { ebf_economy_wealth_tax = 1.00 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_wealth_tax > 0.95 }
						}
					}
					add_to_variable = { ebf_economy_wealth_tax = 0.05 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
				}
            }

			wealth_tax_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_wealth_tax < 0.1 }
					}
					set_variable = { ebf_economy_wealth_tax = 0.00 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_wealth_tax < 0.1 }
						}
					}
					subtract_from_variable = { ebf_economy_wealth_tax = 0.1 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
				}
            }
			wealth_tax_add_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_wealth_tax > 0.90 }
					}
					set_variable = { ebf_economy_wealth_tax = 1.00 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_wealth_tax > 0.90 }
						}
					}
					add_to_variable = { ebf_economy_wealth_tax = 0.1 }
					rich_population = yes
					combined_tax = yes
					monthly_balance = yes
				}
            }

			wealth_tax_sub_button_right_click = {
				set_variable = { ebf_economy_wealth_tax = ebf_economy_default_wealth_tax }
				rich_population = yes
				combined_tax = yes
				monthly_balance = yes
				happines = yes

            }
			wealth_tax_add_button_right_click = {
				set_variable = { ebf_economy_wealth_tax = ebf_economy_default_wealth_tax }
				rich_population = yes
				combined_tax = yes
				monthly_balance = yes
				happines = yes
            }

			### Trade Tax Percentage Click
			trade_tax_sub_button_click = {
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_trade_tax < 0.01 }
						}
					}
					subtract_from_variable = { ebf_economy_trade_tax = 0.01 }
					combined_tax = yes
					monthly_balance = yes
					# GDP = yes
					
				}
            }
			trade_tax_add_button_click = {
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_trade_tax > 0.99 }
						}
					}
					add_to_variable = { ebf_economy_trade_tax = 0.01 }
					combined_tax = yes
					monthly_balance = yes
					# GDP = yes
				}
            }

			trade_tax_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_trade_tax < 0.05 }
					}
					set_variable = { ebf_economy_trade_tax = 0.00 }
					combined_tax = yes
					monthly_balance = yes
					# GDP = yes
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_trade_tax < 0.05 }
						}
					}
					subtract_from_variable = { ebf_economy_trade_tax = 0.05 }
					combined_tax = yes
					monthly_balance = yes
					# GDP = yes
				}
            }
			trade_tax_add_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_trade_tax > 0.95 }
					}
					set_variable = { ebf_economy_trade_tax = 1.00 }
					combined_tax = yes
					monthly_balance = yes
					# GDP = yes
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_trade_tax > 0.95 }
						}
					}
					add_to_variable = { ebf_economy_trade_tax = 0.05 }
					combined_tax = yes
					monthly_balance = yes
					# GDP = yes
				}
            }

			trade_tax_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_trade_tax < 0.1 }
					}
					set_variable = { ebf_economy_trade_tax = 0.00 }
					combined_tax = yes
					monthly_balance = yes
					# GDP = yes
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_trade_tax < 0.1 }
						}
					}
					subtract_from_variable = { ebf_economy_trade_tax = 0.1 }
					combined_tax = yes
					monthly_balance = yes
					# GDP = yes
				}
            }
			trade_tax_add_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_trade_tax > 0.90 }
					}
					set_variable = { ebf_economy_trade_tax = 1.00 }
					combined_tax = yes
					monthly_balance = yes
					# GDP = yes
				}
				if = {
					limit = {
						NOT = {
							check_variable = { ebf_economy_trade_tax > 0.90 }
						}
					}
					add_to_variable = { ebf_economy_trade_tax = 0.1 }
					combined_tax = yes
					monthly_balance = yes
					# GDP = yes
				}
            }

			trade_tax_sub_button_right_click = {
				set_variable = { ebf_economy_trade_tax = ebf_economy_default_trade_tax }
				rich_population = yes
				combined_tax = yes
				monthly_balance = yes
				happines = yes

            }
			trade_tax_add_button_right_click = {
				set_variable = { ebf_economy_trade_tax = ebf_economy_default_trade_tax }
				rich_population = yes
				combined_tax = yes
				monthly_balance = yes
				happines = yes
            }

			### Money Printing
			money_printing_sub_button_click = {
				subtract_from_variable = { ebf_economy_money_printing_rate = 0.001 }
				relative_printing_rate = yes
				national_economy_money = yes
				money_printing_amount = yes
				monthly_balance = yes
				printing_inflation = yes
            }
			
			money_printing_sub_button_shift_click = {
				subtract_from_variable = { ebf_economy_money_printing_rate = 0.01 }
				relative_printing_rate = yes
				national_economy_money = yes
				money_printing_amount = yes
				monthly_balance = yes
				printing_inflation = yes
            }

			money_printing_add_button_click = {
				add_to_variable = { ebf_economy_money_printing_rate = 0.001 }
				relative_printing_rate = yes
				national_economy_money = yes
				money_printing_amount = yes
				monthly_balance = yes
				printing_inflation = yes
            }
			
			money_printing_add_button_shift_click = {
				add_to_variable = { ebf_economy_money_printing_rate = 0.01 }
				relative_printing_rate = yes
				national_economy_money = yes
				money_printing_amount = yes
				monthly_balance = yes
				printing_inflation = yes
            }

			######### Expenses

			### Civilian Healthcare
			healthcare_add_button_click = {
				add_to_variable = { ebf_economy_healthcare_rate = 1 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
            }

			healthcare_add_button_control_click = {
				add_to_variable = { ebf_economy_healthcare_rate = 10 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
            }

			healthcare_add_button_shift_click = {
				add_to_variable = { ebf_economy_healthcare_rate = 100 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
            }

			healthcare_add_button_right_click = {
				set_variable = { ebf_economy_healthcare_rate = ebf_economy_recommended_healthcare_rate }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
            }

			healthcare_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_healthcare_rate > 0 }
					}
					subtract_from_variable = { ebf_economy_healthcare_rate = 1 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
            }

			healthcare_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_healthcare_rate < 9 }
					}
					set_variable = { ebf_economy_healthcare_rate = 0 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_healthcare_rate = 10 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes	
				}
            }

			healthcare_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_healthcare_rate < 99 }
					}
					set_variable = { ebf_economy_healthcare_rate = 0 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_healthcare_rate = 100 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes	
				}
            }

			healthcare_sub_button_right_click = {
				set_variable = { ebf_economy_healthcare_rate = ebf_economy_recommended_healthcare_rate }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
            }

			### Military Healthcare
			soldiers_healthcare_add_button_click = {
				add_to_variable = { ebf_economy_soldiers_healthcare_rate = 1 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
            }

			soldiers_healthcare_add_button_control_click = {
				add_to_variable = { ebf_economy_soldiers_healthcare_rate = 10 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
            }

			soldiers_healthcare_add_button_shift_click = {
				add_to_variable = { ebf_economy_soldiers_healthcare_rate = 100 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
            }

			soldiers_healthcare_add_button_right_click = {
				set_variable = { ebf_economy_soldiers_healthcare_rate = ebf_economy_recommended_soldiers_healthcare_rate }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
            }

			soldiers_healthcare_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_soldiers_healthcare_rate > 0 }
					}
					subtract_from_variable = { ebf_economy_soldiers_healthcare_rate = 1 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
            }

			soldiers_healthcare_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_soldiers_healthcare_rate < 9 }
					}
					set_variable = { ebf_economy_soldiers_healthcare_rate = 0 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_soldiers_healthcare_rate = 10 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes	
				}
            }

			soldiers_healthcare_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_soldiers_healthcare_rate < 99 }
					}
					set_variable = { ebf_economy_soldiers_healthcare_rate = 0 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_soldiers_healthcare_rate = 100 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes	
				}
            }

			soldiers_healthcare_sub_button_right_click = {
				set_variable = { ebf_economy_soldiers_healthcare_rate = ebf_economy_recommended_soldiers_healthcare_rate }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
            }

			### Pension
			pension_add_button_click = {
				add_to_variable = { ebf_economy_pension_rate = 1 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			pension_add_button_control_click = {
				add_to_variable = { ebf_economy_pension_rate = 10 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			pension_add_button_shift_click = {
				add_to_variable = { ebf_economy_pension_rate = 100 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			pension_add_button_right_click = {
				set_variable = { ebf_economy_pension_rate = ebf_economy_recommended_pension_rate }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			pension_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_pension_rate > 0 }
					}
					subtract_from_variable = { ebf_economy_pension_rate = 1 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
			}

			pension_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_pension_rate < 9 }
					}
					set_variable = { ebf_economy_pension_rate = 0 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_pension_rate = 10 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes	
				}
			}

			pension_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_pension_rate < 99 }
					}
					set_variable = { ebf_economy_pension_rate = 0 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_pension_rate = 100 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes	
				}
			}

			pension_sub_button_right_click = {
				set_variable = { ebf_economy_pension_rate = ebf_economy_recommended_pension_rate }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			### Saleries
			saleries_add_button_click = {
				add_to_variable = { ebf_economy_saleries_rate = 1 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			saleries_add_button_control_click = {
				add_to_variable = { ebf_economy_saleries_rate = 10 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			saleries_add_button_shift_click = {
				add_to_variable = { ebf_economy_saleries_rate = 100 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			saleries_add_button_right_click = {
				set_variable = { ebf_economy_saleries_rate = ebf_economy_recommended_saleries_rate }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			saleries_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_saleries_rate > 0 }
					}
					subtract_from_variable = { ebf_economy_saleries_rate = 1 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
			}

			saleries_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_saleries_rate < 9 }
					}
					set_variable = { ebf_economy_saleries_rate = 0 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_saleries_rate = 10 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes	
				}
			}

			saleries_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_saleries_rate < 99 }
					}
					set_variable = { ebf_economy_saleries_rate = 0 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_saleries_rate = 100 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes	
				}
			}

			saleries_sub_button_right_click = {
				set_variable = { ebf_economy_saleries_rate = ebf_economy_recommended_saleries_rate }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			### Social Welfare
			social_welfare_add_button_click = {
				add_to_variable = { ebf_economy_social_welfare_rate = 1 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			social_welfare_add_button_control_click = {
				add_to_variable = { ebf_economy_social_welfare_rate = 10 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			social_welfare_add_button_shift_click = {
				add_to_variable = { ebf_economy_social_welfare_rate = 100 }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			social_welfare_add_button_right_click = {
				set_variable = { ebf_economy_social_welfare_rate = ebf_economy_recommended_social_welfare_rate }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}

			social_welfare_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_social_welfare_rate > 0 }
					}
					subtract_from_variable = { ebf_economy_social_welfare_rate = 1 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
			}

			social_welfare_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_social_welfare_rate < 9 }
					}
					set_variable = { ebf_economy_social_welfare_rate = 0 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_social_welfare_rate = 10 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes	
				}
			}

			social_welfare_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_social_welfare_rate < 99 }
					}
					set_variable = { ebf_economy_social_welfare_rate = 0 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_social_welfare_rate = 100 }
					total_expenses = yes
					monthly_balance = yes
					happines = yes	
				}
			}

			social_welfare_sub_button_right_click = {
				set_variable = { ebf_economy_social_welfare_rate = ebf_economy_recommended_social_welfare_rate }
				total_expenses = yes
				monthly_balance = yes
				happines = yes
			}
			
			##        #######     ###    ##    ##  
			##       ##     ##   ## ##   ###   ##  
			##       ##     ##  ##   ##  ####  ##  
			##       ##     ## ##     ## ## ## ##  
			##       ##     ## ######### ##  ####  
			##       ##     ## ##     ## ##   ###  
			########  #######  ##     ## ##    ##  

			### Loan Repayment
			loan_repayment_add_button_click = {
				add_to_variable = { ebf_economy_loan_monthly_repay_amount = 10 }
				monthly_repay_amount = yes
				monthly_balance = yes
			}

			loan_repayment_add_button_control_click = {
				add_to_variable = { ebf_economy_loan_monthly_repay_amount = 100 }
				monthly_repay_amount = yes
				monthly_balance = yes
			}

			loan_repayment_add_button_shift_click = {
				add_to_variable = { ebf_economy_loan_monthly_repay_amount = 1000 }
				monthly_repay_amount = yes
				monthly_balance = yes
			}

			loan_repayment_add_button_alt_click = {
				add_to_variable = { ebf_economy_loan_monthly_repay_amount = 10000 }
				monthly_repay_amount = yes
				monthly_balance = yes
			}

			loan_repayment_add_button_right_click = {
				set_variable = { ebf_economy_loan_monthly_repay_amount = 0 }
				monthly_repay_amount = yes
				monthly_balance = yes
			}

			loan_repayment_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_loan_monthly_repay_amount > 9 }
					}
					subtract_from_variable = { ebf_economy_loan_monthly_repay_amount = 10 }
					monthly_repay_amount = yes
				monthly_balance = yes
				}
			}
						
			loan_repayment_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_loan_monthly_repay_amount < 99 }
					}
					set_variable = { ebf_economy_loan_monthly_repay_amount = 0 }
					monthly_repay_amount = yes
				monthly_balance = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_loan_monthly_repay_amount = 100 }
					monthly_repay_amount = yes
				monthly_balance = yes	
				}
			}

			loan_repayment_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_loan_monthly_repay_amount < 999 }
					}
					set_variable = { ebf_economy_loan_monthly_repay_amount = 0 }
					monthly_repay_amount = yes
				monthly_balance = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_loan_monthly_repay_amount = 1000 }
					monthly_repay_amount = yes
				monthly_balance = yes
				}
			}

			loan_repayment_sub_button_alt_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_loan_monthly_repay_amount < 9999 }
					}
					set_variable = { ebf_economy_loan_monthly_repay_amount = 0 }
					monthly_repay_amount = yes
				monthly_balance = yes
				}
				else = {
					subtract_from_variable = { ebf_economy_loan_monthly_repay_amount = 10000 }
					monthly_repay_amount = yes
				monthly_balance = yes
				}
			}

			loan_repayment_sub_button_right_click = {
				set_variable = { ebf_economy_loan_monthly_repay_amount = 0 }
				monthly_repay_amount = yes
				monthly_balance = yes
			}

			# Take Loan
			loan_repayment_take_loan_button_click = {
				set_temp_variable = { ebf_economy_loan_cap_adjusted = ebf_economy_loan_cap }
				subtract_from_variable = { ebf_economy_loan_cap_adjusted = 10 }
				if = {
					limit = {
						check_variable = { ebf_economy_loan_amount < ebf_economy_loan_cap_adjusted }
					}
					add_to_variable = { ebf_economy_loan_amount = 10 }
					add_to_variable = { ebf_economy_money = 10 }
					loan_income_ratio = yes
					interest_rate = yes
					interest_amount = yes
					monthly_balance = yes
				}
			}

			loan_repayment_take_loan_button_control_click = {
				set_temp_variable = { ebf_economy_loan_cap_adjusted = ebf_economy_loan_cap }
				subtract_from_variable = { ebf_economy_loan_cap_adjusted = 100 }
				if = {
					limit = {
						check_variable = { ebf_economy_loan_amount < ebf_economy_loan_cap_adjusted }
					}
					add_to_variable = { ebf_economy_loan_amount = 100 }
					add_to_variable = { ebf_economy_money = 100 }
					loan_income_ratio = yes
					interest_rate = yes
					interest_amount = yes
					monthly_balance = yes
				}
			}

			loan_repayment_take_loan_button_shift_click = {
				set_temp_variable = { ebf_economy_loan_cap_adjusted = ebf_economy_loan_cap }
				subtract_from_variable = { ebf_economy_loan_cap_adjusted = 1000 }
				if = {
					limit = {
						check_variable = { ebf_economy_loan_amount < ebf_economy_loan_cap_adjusted }
					}
					add_to_variable = { ebf_economy_loan_amount = 1000 }
					add_to_variable = { ebf_economy_money = 1000 }
					loan_income_ratio = yes
					interest_rate = yes
					interest_amount = yes
					monthly_balance = yes
				}
			}

			# One time payment
			loan_repayment_take_loan_button_right_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_loan_amount > 10 }
					}
					set_variable = { ebf_economy_loan_one_time_repay_amount = 10 }
					pay_one_time = yes
					loan_income_ratio = yes
					interest_rate = yes
					interest_amount = yes
					monthly_balance = yes
				}
				else = {
					set_variable = { ebf_economy_loan_one_time_repay_amount = ebf_economy_loan_amount }
					pay_one_time = yes
					loan_income_ratio = yes
					interest_rate = yes
					interest_amount = yes
					monthly_balance = yes
				}
			}

			loan_repayment_take_loan_button_control_right_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_loan_amount > 100 }
					}
					set_variable = { ebf_economy_loan_one_time_repay_amount = 100 }
					pay_one_time = yes
					loan_income_ratio = yes
					interest_rate = yes
					interest_amount = yes
					monthly_balance = yes
				}
				else = {
					set_variable = { ebf_economy_loan_one_time_repay_amount = ebf_economy_loan_amount }
					pay_one_time = yes
					loan_income_ratio = yes
					interest_rate = yes
					interest_amount = yes
					monthly_balance = yes
				}
			}

			loan_repayment_take_loan_button_shift_right_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_loan_amount > 1000 }
					}
					set_variable = { ebf_economy_loan_one_time_repay_amount = 1000 }
					pay_one_time = yes
					loan_income_ratio = yes
					interest_rate = yes
					interest_amount = yes
					monthly_balance = yes
				}
				else = {
					set_variable = { ebf_economy_loan_one_time_repay_amount = ebf_economy_loan_amount }
					pay_one_time = yes
					loan_income_ratio = yes
					interest_rate = yes
					interest_amount = yes
					monthly_balance = yes
				}
			}

			########  ##     ## ########   ######   ######## ########  ######  
			##     ## ##     ## ##     ## ##    ##  ##          ##    ##    ## 
			##     ## ##     ## ##     ## ##        ##          ##    ##       
			########  ##     ## ##     ## ##   #### ######      ##     ######  
			##     ## ##     ## ##     ## ##    ##  ##          ##          ## 
			##     ## ##     ## ##     ## ##    ##  ##          ##    ##    ## 
			########   #######  ########   ######   ########    ##     ###### 

			### Police Budget
			police_budget_add_button_click = {
				add_to_variable = { ebf_economy_police_budget = 1 }
				calculate_police_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			police_budget_add_button_control_click = {
				add_to_variable = { ebf_economy_police_budget = 10 }
				calculate_police_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			police_budget_add_button_shift_click = {
				add_to_variable = { ebf_economy_police_budget = 100 }
				calculate_police_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			police_budget_add_button_right_click = {
				set_variable = { ebf_economy_police_budget = ebf_economy_recommended_police_budget }
				calculate_police_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			police_budget_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_police_budget > 0 }
					}
					subtract_from_variable = { ebf_economy_police_budget = 1 }
					calculate_police_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
			}

			police_budget_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_police_budget < 9 }
					}
					set_variable = { ebf_economy_police_budget = 0 }
					calculate_police_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
				else = {
					subtract_from_variable = { ebf_economy_police_budget = 10 }
					calculate_police_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			police_budget_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_police_budget < 99 }
					}
					set_variable = { ebf_economy_police_budget = 0 }
					calculate_police_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
				else = {
					subtract_from_variable = { ebf_economy_police_budget = 100 }
					calculate_police_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			police_budget_sub_button_right_click = {
				set_variable = { ebf_economy_police_budget = ebf_economy_recommended_police_budget }
				calculate_police_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework	
			}

			### Research Budget
			research_budget_add_button_click = {
				add_to_variable = { ebf_economy_research_budget = 1 }
				calculate_research_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			research_budget_add_button_control_click = {
				add_to_variable = { ebf_economy_research_budget = 10 }
				calculate_research_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			research_budget_add_button_shift_click = {
				add_to_variable = { ebf_economy_research_budget = 100 }
				calculate_research_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			research_budget_add_button_right_click = {
				set_variable = { ebf_economy_research_budget = ebf_economy_recommended_research_budget }
				calculate_research_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			research_budget_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_research_budget > 0 }
					}
					subtract_from_variable = { ebf_economy_research_budget = 1 }
					calculate_research_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
			}

			research_budget_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_research_budget < 9 }
					}
					set_variable = { ebf_economy_research_budget = 0 }
					calculate_research_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
				else = {
					subtract_from_variable = { ebf_economy_research_budget = 10 }
					calculate_research_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			research_budget_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_research_budget < 99 }
					}
					set_variable = { ebf_economy_research_budget = 0 }
					calculate_research_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
				else = {
					subtract_from_variable = { ebf_economy_research_budget = 100 }
					calculate_research_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			research_budget_sub_button_right_click = {
				set_variable = { ebf_economy_research_budget = ebf_economy_recommended_research_budget }
				calculate_research_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework	
			}

			### Military Budget
			military_budget_add_button_click = {
				add_to_variable = { ebf_economy_military_budget = 1 }
				calculate_military_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			military_budget_add_button_control_click = {
				add_to_variable = { ebf_economy_military_budget = 10 }
				calculate_military_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			military_budget_add_button_shift_click = {
				add_to_variable = { ebf_economy_military_budget = 100 }
				calculate_military_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			military_budget_add_button_right_click = {
				set_variable = { ebf_economy_military_budget = ebf_economy_recommended_military_budget }
				calculate_military_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			military_budget_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_military_budget > 0 }
					}
					subtract_from_variable = { ebf_economy_military_budget = 1 }
					calculate_military_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
			}

			military_budget_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_military_budget < 9 }
					}
					set_variable = { ebf_economy_military_budget = 0 }
					calculate_military_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
				else = {
					subtract_from_variable = { ebf_economy_military_budget = 10 }
					calculate_military_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			military_budget_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_military_budget < 99 }
					}
					set_variable = { ebf_economy_military_budget = 0 }
					calculate_military_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
				else = {
					subtract_from_variable = { ebf_economy_military_budget = 100 }
					calculate_military_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			military_budget_sub_button_right_click = {
				set_variable = { ebf_economy_military_budget = ebf_economy_recommended_military_budget }
				calculate_military_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework	
			}
			
			### Bureacracy Budget
			intelligence_budget_add_button_click = {
				add_to_variable = { ebf_economy_intelligence_budget = 1 }
				calculate_intelligence_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			intelligence_budget_add_button_control_click = {
				add_to_variable = { ebf_economy_intelligence_budget = 10 }
				calculate_intelligence_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			intelligence_budget_add_button_shift_click = {
				add_to_variable = { ebf_economy_intelligence_budget = 100 }
				calculate_intelligence_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			intelligence_budget_add_button_right_click = {
				set_variable = { ebf_economy_intelligence_budget = ebf_economy_intelligence_budget_expecteds }
				calculate_intelligence_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			intelligence_budget_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_intelligence_budget > 0 }
					}
					subtract_from_variable = { ebf_economy_intelligence_budget = 1 }
					calculate_intelligence_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
			}

			intelligence_budget_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_intelligence_budget < 9 }
					}
					set_variable = { ebf_economy_intelligence_budget = 0 }
					calculate_intelligence_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
				else = {
					subtract_from_variable = { ebf_economy_intelligence_budget = 10 }
					calculate_intelligence_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			intelligence_budget_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_intelligence_budget < 99 }
					}
					set_variable = { ebf_economy_intelligence_budget = 0 }
					calculate_intelligence_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
				else = {
					subtract_from_variable = { ebf_economy_intelligence_budget = 100 }
					calculate_intelligence_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			intelligence_budget_sub_button_right_click = {
				set_variable = { ebf_economy_intelligence_budget = ebf_economy_intelligence_budget_expected }
				calculate_intelligence_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework	
			}

			### Construction Budget
			construction_budget_add_button_click = {
				add_to_variable = { ebf_economy_construction_budget = 1 }
				calculate_construction_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			construction_budget_add_button_control_click = {
				add_to_variable = { ebf_economy_construction_budget = 10 }
				calculate_construction_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			construction_budget_add_button_shift_click = {
				add_to_variable = { ebf_economy_construction_budget = 100 }
				calculate_construction_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			construction_budget_add_button_right_click = {
				set_variable = { ebf_economy_construction_budget = ebf_economy_recommended_construction_budget }
				calculate_construction_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			construction_budget_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_construction_budget > 0 }
					}
					subtract_from_variable = { ebf_economy_construction_budget = 1 }
					calculate_construction_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
			}

			construction_budget_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_construction_budget < 9 }
					}
					set_variable = { ebf_economy_construction_budget = 0 }
					calculate_construction_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
				else = {
					subtract_from_variable = { ebf_economy_construction_budget = 10 }
					calculate_construction_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			construction_budget_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_construction_budget < 99 }
					}
					set_variable = { ebf_economy_construction_budget = 0 }
					calculate_construction_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
				else = {
					subtract_from_variable = { ebf_economy_construction_budget = 100 }
					calculate_construction_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			construction_budget_sub_button_right_click = {
				set_variable = { ebf_economy_construction_budget = ebf_economy_recommended_construction_budget }
				calculate_construction_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework	
			}
			
			### Social Welfare Budget
			social_welfare_budget_add_button_click = {
				add_to_variable = { ebf_economy_social_welfare_budget = 1 }
				calculate_social_welfare_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			social_welfare_budget_add_button_control_click = {
				add_to_variable = { ebf_economy_social_welfare_budget = 10 }
				calculate_social_welfare_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			social_welfare_budget_add_button_shift_click = {
				add_to_variable = { ebf_economy_social_welfare_budget = 100 }
				calculate_social_welfare_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			social_welfare_budget_add_button_right_click = {
				set_variable = { ebf_economy_social_welfare_budget = ebf_economy_recommended_social_welfare_budget }
				calculate_social_welfare_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			social_welfare_budget_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_social_welfare_budget > 0 }
					}
					subtract_from_variable = { ebf_economy_social_welfare_budget = 1 }
					calculate_social_welfare_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
			}

			social_welfare_budget_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_social_welfare_budget < 9 }
					}
					set_variable = { ebf_economy_social_welfare_budget = 0 }
					calculate_social_welfare_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
				else = {
					subtract_from_variable = { ebf_economy_social_welfare_budget = 10 }
					calculate_social_welfare_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			social_welfare_budget_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_social_welfare_budget < 99 }
					}
					set_variable = { ebf_economy_social_welfare_budget = 0 }
					calculate_social_welfare_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
				else = {
					subtract_from_variable = { ebf_economy_social_welfare_budget = 100 }
					calculate_social_welfare_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			social_welfare_budget_sub_button_right_click = {
				set_variable = { ebf_economy_social_welfare_budget = ebf_economy_recommended_social_welfare_budget }
				calculate_social_welfare_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework	
			}

			### Healthcare Budget
			healthcare_budget_add_button_click = {
				add_to_variable = { ebf_economy_healthcare_budget = 1 }
				calculate_healthcare_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			healthcare_budget_add_button_control_click = {
				add_to_variable = { ebf_economy_healthcare_budget = 10 }
				calculate_healthcare_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			healthcare_budget_add_button_shift_click = {
				add_to_variable = { ebf_economy_healthcare_budget = 100 }
				calculate_healthcare_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			healthcare_budget_add_button_right_click = {
				set_variable = { ebf_economy_healthcare_budget = ebf_economy_recommended_healthcare_budget }
				calculate_healthcare_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework
			}

			healthcare_budget_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_healthcare_budget > 0 }
					}
					subtract_from_variable = { ebf_economy_healthcare_budget = 1 }
					calculate_healthcare_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
			}

			healthcare_budget_sub_button_control_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_healthcare_budget < 9 }
					}
					set_variable = { ebf_economy_healthcare_budget = 0 }
					calculate_healthcare_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework
				}
				else = {
					subtract_from_variable = { ebf_economy_healthcare_budget = 10 }
					calculate_healthcare_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			healthcare_budget_sub_button_shift_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_healthcare_budget < 99 }
					}
					set_variable = { ebf_economy_healthcare_budget = 0 }
					calculate_healthcare_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
				else = {
					subtract_from_variable = { ebf_economy_healthcare_budget = 100 }
					calculate_healthcare_budget_modifiers = yes
					calculate_total_budget_cost = yes
					monthly_balance = yes
					happines = yes # Will have to wait for a Happines Rework	
				}
			}

			healthcare_budget_sub_button_right_click = {
				set_variable = { ebf_economy_healthcare_budget = ebf_economy_recommended_healthcare_budget }
				calculate_healthcare_budget_modifiers = yes
				calculate_total_budget_cost = yes
				monthly_balance = yes
				happines = yes # Will have to wait for a Happines Rework	
			}
			
			######### Society

			### Full Age
			full_age_add_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_age_of_majority < ebf_economy_retirement_age }
					}
					add_to_variable = { ebf_economy_age_of_majority = 1 }
					adult_population = yes
					unemployed_population = yes
					workforce_population = yes
					rich_population = yes
				}
			}

			full_age_add_button_right_click = {
				set_variable = { ebf_economy_age_of_majority = ebf_economy_default_age_of_majority }
				adult_population = yes
				unemployed_population = yes
				workforce_population = yes
				rich_population = yes
			}

			full_age_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_age_of_majority > 0 }
					}
					subtract_from_variable = { ebf_economy_age_of_majority = 1 }
					adult_population = yes
					unemployed_population = yes
					workforce_population = yes
					rich_population = yes
				}
			}

			full_age_sub_button_right_click = {
				set_variable = { ebf_economy_age_of_majority = ebf_economy_default_age_of_majority }
				adult_population = yes
				unemployed_population = yes
				workforce_population = yes
				rich_population = yes
			}


			### Retirement Age
			retirement_age_add_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_retirement_age < ebf_economy_life_expectancy }
					}
					add_to_variable = { ebf_economy_retirement_age = 1 }
					adult_population = yes
					unemployed_population = yes
					workforce_population = yes
					rich_population = yes
				}
			}

			retirement_age_add_button_right_click = {
				set_variable = { ebf_economy_retirement_age = ebf_economy_default_retirement_age }
				adult_population = yes
				unemployed_population = yes
				workforce_population = yes
				rich_population = yes
			}

			retirement_age_sub_button_click = {
				if = {
					limit = {
						check_variable = { ebf_economy_retirement_age > ebf_economy_age_of_majority }
					}
					subtract_from_variable = { ebf_economy_retirement_age = 1 }
					adult_population = yes
					unemployed_population = yes
					workforce_population = yes
					rich_population = yes
				}
			}

			retirement_age_sub_button_right_click = {
				set_variable = { ebf_economy_retirement_age = ebf_economy_default_age_of_majority }
				adult_population = yes
				unemployed_population = yes
				workforce_population = yes
				rich_population = yes
			}




		}

		dynamic_lists = {
			interest_rate_line_segments = {
				array = ebf_economy_intrest_rate_graph_offsets  # SAMPLE_graph_offsets			# See next section, this is one of the arrays which defines your line graph
				change_scope = no
				entry_container = intrest_rate_graph_line_segment	# Container name from previous step
				index = ebf_economy_intrest_rate_graph_segment_index				# This variable can be used in scripted effects, loc, or GUI to change
										# behavior based on the current segment
			}
		}

		triggers = {
			entry_building_expenses_visible = {
				always = no
			}
		}

		properties = {
			# Here, segment is the name of the iconType in your container
			intrest_rate_graph_segment = {
				# Sets the relative position of the line segment to the graph offset
				# This graph offset is equal to to -y1
				y = ebf_economy_intrest_rate_graph_offsets^ebf_economy_intrest_rate_graph_segment_index
		
				# Tells the shader how steep to draw the line
				# Must be equal to (y2 - y1) / 2 + 50
				frame = ebf_economy_intrest_rate_graph_slopes^ebf_economy_intrest_rate_graph_segment_index
			}
		}
	}

	economic_building_expenses_ui = {
		context_type = player_context
		window_name = "entry_building_expenses"

		visible = {
			always = no
		}
	}

	economic_building_expenses_extended_ui = {
		context_type = player_context
		window_name = "entry_building_expenses_extended"

		visible = {
			always = no
		}
	}
}